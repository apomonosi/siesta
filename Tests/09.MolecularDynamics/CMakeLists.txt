#
siesta_subtest(verlet LABELS simple)
siesta_subtest(anneal FILES_CLEANUP anneal.ANNEAL_RESTART)
siesta_subtest(anneal_pt FILES_CLEANUP anneal_pt.ANNEAL_RESTART)
siesta_subtest(nose)
siesta_subtest(pr)
siesta_subtest(npr)
siesta_subtest(npr_rip)

siesta_subtest(verlet_restart    NAME_ALTER verlet_rst    FILES_SETUP verlet.XV verlet.VERLET_RESTART LABELS simple)
siesta_subtest(anneal_restart    NAME_ALTER anneal_rst    FILES_SETUP anneal.XV anneal.ANNEAL_RESTART)
siesta_subtest(anneal_pt_restart NAME_ALTER anneal_pt_rst FILES_SETUP anneal_pt.XV anneal_pt.ANNEAL_RESTART)
siesta_subtest(nose_restart      NAME_ALTER nose_rst      FILES_SETUP nose.XV nose.NOSE_RESTART)
siesta_subtest(pr_restart        NAME_ALTER pr_rst        FILES_SETUP pr.XV pr.PR_RESTART)
siesta_subtest(npr_restart       NAME_ALTER npr_rst       FILES_SETUP npr.XV npr.NPR_RESTART)
siesta_subtest(npr_rip_restart   NAME_ALTER npr_rip_rst   FILES_SETUP npr_rip.XV npr_rip.NPR_RESTART)

siesta_subtest(verlet_restart    NAME_ALTER verlet_rstxv    FILES_SETUP verlet.XV FILES_CLEANUP verlet.VERLET_RESTART LABELS simple)
siesta_subtest(anneal_restart    NAME_ALTER anneal_rstxv    FILES_SETUP anneal.XV FILES_CLEANUP anneal.ANNEAL_RESTART)
siesta_subtest(anneal_pt_restart NAME_ALTER anneal_pt_rstxv FILES_SETUP anneal_pt.XV FILES_CLEANUP anneal_pt.ANNEAL_RESTART)
siesta_subtest(nose_restart      NAME_ALTER nose_rstxv      FILES_SETUP nose.XV FILES_CLEANUP nose.NOSE_RESTART)
siesta_subtest(pr_restart        NAME_ALTER pr_rstxv        FILES_SETUP pr.XV FILES_CLEANUP pr.PR_RESTART)
siesta_subtest(npr_restart       NAME_ALTER npr_rstxv       FILES_SETUP npr.XV FILES_CLEANUP npr.NPR_RESTART)
siesta_subtest(npr_rip_restart   NAME_ALTER npr_rip_rstxv   FILES_SETUP npr_rip.XV FILES_CLEANUP npr_rip.NPR_RESTART)